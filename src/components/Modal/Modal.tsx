import React from 'react';

type ModalProps = {
    title: string;
};

const Modal = ({title}: ModalProps) => {

    return (
        <div className={'inset-0'}>

        </div>
    );
};

export default Modal;